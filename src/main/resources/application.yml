spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/springsecuiry
    username: matheus
    password: 101110
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
        ddl-auto: update
    show-sql: true
    properties:
        hibernate:
          dialect: org.hibernate.dialect.PostgreSQLDialect